<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.1.3/dist/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
</head>
<body>
  <h3>User: <%= user.UserProfile.name %></h3>
  <h3>Saldo: <%= user.UserProfile.saldo %></h3>
  <table border="1">
    <thead>
      <tr>
        <th>No</th>
        <th>Name</th>
        <th>Category</th>
        <th>Brand</th>
        <th>Stock</th>
        <th>Price</th>
        <th>Description</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% let counter = 0; %>
      <% products.forEach(v =>{ %>
        <% counter++; %>
        <tr>
          <td><%= counter %></td>
          <td><%= v.name %></td>
          <td><%= v.Category.name %></td>
          <td><%= v.brand %></td>
          <td><%= v.stock %></td>
          <td><%= v.price %></td>
          <td><%= v.description %></td>
          <td>
            <% if (v.stock > 0) { %> 
              <a href="/dashboard/user/buyItem/<%= v.id %> " class="btn btn-primary">Buy</a></td>
            <% } else { %> 
              <span>Item out of stock</span>
            <% } %> 
        </tr>
      <% }) %> 
    </tbody>
  </table>

  <br>
  <br>
  </table>
  <button><a href="/logout">Logout</a></button>
</body>
</html>